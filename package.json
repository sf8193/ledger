{
  "name": "ledger",
  "private": true,
  "version": "1.0.0",
  "description": "Self-hosted personal finance app",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "PORT=${PORT:-4100} VITE_PORT=${VITE_PORT:-5180} VITE_API_URL=http://localhost:${PORT:-4100} FRONTEND_URL=http://localhost:${VITE_PORT:-5180} concurrently -n api,web -c blue,green \"pnpm dev:api\" \"pnpm dev:web\"",
    "dev:api": "pnpm --filter @ledger/api dev",
    "dev:web": "pnpm --filter @ledger/web dev",
    "build": "pnpm -r build"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "playwright": "^1.62.1"
  }
}
